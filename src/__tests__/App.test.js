import React from 'react';
import App from '../components/App/index';
import renderer from 'react-test-renderer';

describe('The sign in form', () => {
    it('...should fail...', () => {
        expect(false).toBeFalsy()
    })
	it('...should sometimes succeed ;)', () => {
		expect(true).toBeFalsy()
	})
});